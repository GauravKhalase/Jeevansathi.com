import React from "react";

const Notifications = () => {
  return (
    <div>
      <div>{/* onClick={() => navigate(-1)} */}</div>
      <div>Notifications</div>
    </div>
  );
};

export default Notifications;
